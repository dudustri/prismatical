#include "fractal.h"

Fractal::Fractal(unsigned int seed) : seed_(seed) {}

void Fractal::generate() {
    // TODO: generate fractal geometry from seed
    // Ideas: Mandelbrot, Julia sets, L-systems
}
